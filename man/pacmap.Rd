% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacmap.R
\name{pacmap}
\alias{pacmap}
\title{Fit a PaCMAP embedding}
\usage{
pacmap(
  X,
  n_components = 2L,
  n_neighbors = NULL,
  MN_ratio = 0.5,
  FP_ratio = 2,
  distance = c("euclidean", "manhattan", "angular", "hamming", "precomputed"),
  lr = 1,
  num_iters = c(100L, 100L, 250L),
  init = "pca",
  apply_pca = TRUE,
  ann_backend = c("hnsw", "faiss", "auto"),
  n_threads = 1L,
  random_state = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X}{a numeric matrix (rows = observations, cols = features).}

\item{n_components}{dimension of the output embedding (default 2).}

\item{n_neighbors}{number of nearest neighbours for the NN pair term. If
\code{NULL}, uses the heuristic from Wang et al. (10 for n<=10000, else
\code{round(10 + 15 * (log10(n) - 4))}).}

\item{MN_ratio}{ratio of mid-near pairs to NN pairs (default 0.5).}

\item{FP_ratio}{ratio of further pairs to NN pairs (default 2.0).}

\item{distance}{one of "euclidean", "manhattan", "angular", "hamming", or
"precomputed". With "precomputed", \code{X} is treated as a square
distance matrix (n x n); \code{apply_pca} is ignored and \code{init}
defaults to "random". \code{transform()} is not supported in this mode.}

\item{lr}{Adam learning rate (default 1.0).}

\item{num_iters}{length-3 integer vector or single integer. If single, uses
\code{c(100, 100, num_iters)} to mirror the Python default.}

\item{init}{"pca" (default), "random", or a numeric matrix of shape
\code{c(nrow(X), n_components)}.}

\item{apply_pca}{logical, apply PCA-to-100 preprocessing when ncol(X)>100.}

\item{ann_backend}{ANN backend for neighbour search. Currently "hnsw".}

\item{n_threads}{threads for the ANN build/query step.}

\item{random_state}{integer seed for deterministic pair sampling. If NULL,
pair sampling uses R's RNG.}

\item{verbose}{print progress.}
}
\value{
An S3 object of class "pacmap" with fields
  \code{embedding}, \code{loss}, \code{pair_neighbors}, \code{pair_MN},
  \code{pair_FP}, and \code{params}.
}
\description{
PaCMAP (Pairwise Controlled Manifold Approximation Projection) maps a
high-dimensional dataset to a low-dimensional embedding by simultaneously
optimizing over three types of pairs: nearest-neighbour, mid-near, and
further pairs.
}
\details{
This is a native R + Rcpp implementation. No Python required.
}
\examples{
\dontrun{
  emb <- pacmap(as.matrix(iris[, 1:4]), random_state = 42L)
  plot(emb$embedding, col = iris$Species)
}
}
